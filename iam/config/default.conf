# Default configuration
MAX_PARALLEL_JOBS=10
CACHE_TTL=3600
MAX_RETRIES=3
RETRY_DELAY=5
MAX_ERRORS=5
BACKUP_DIR="${HOME}/Desktop/gcp-iam-backups"
DEBUG=false 

# Cache settings
CACHE_TTL=3600  # 1 hour in seconds
CACHE_COMPRESSION=true
CACHE_DIR="${HOME}/.cache/gcp-toolkit/iam"

# Backup settings
BACKUP_MAX_AGE=30  # days
MAX_BACKUPS=10

# Performance settings
MAX_PARALLEL_JOBS=10
MAX_RETRIES=3
RETRY_DELAY=5
MAX_ERRORS=5

# Lock settings
LOCK_WAIT_TIME=30  # seconds
LOCK_RETRY_INTERVAL=0.1

# Debug settings
DRY_RUN=false

# Colors (can be overridden by user)
COLOR_RED='\033[0;31m'
COLOR_GREEN='\033[0;32m'
COLOR_YELLOW='\033[1;33m'
COLOR_RESET='\033[0m'

# Performance tuning
BATCH_SIZE=5
MAX_MEMORY_MB=1024
MEMORY_CHECK_INTERVAL=60
MAX_CONNECTIONS=5
CONNECTION_TIMEOUT=300
UPDATE_INTERVAL=0.2

# Performance optimization
PREFETCH_BATCH_SIZE=20
JQ_BATCH_SIZE=1000
IO_BUFFER_SIZE=$((16*1024))
PROCESS_PRIORITY=-10
MAX_FILE_DESCRIPTORS=4096
USE_RAMDISK=true

# Advanced optimization
MIN_BATCH_SIZE=5
MAX_BATCH_SIZE=50
KEEPALIVE_INTERVAL=60
TCP_RETRIES=3
BUFFER_SIZE=$((1024 * 1024))
FLUSH_THRESHOLD=$((BUFFER_SIZE * 0.8))
CPU_THRESHOLD=80
MEMORY_THRESHOLD=80
RESOURCE_CHECK_INTERVAL=5

# Advanced optimization settings
QUEUE_COUNT=$(nproc)
TARGET_LATENCY=100
CONCURRENCY_WINDOW=10
LOAD_THRESHOLD=0.8
IO_THRESHOLD=0.7
NET_THRESHOLD=0.6
PREFETCH_MAX=5